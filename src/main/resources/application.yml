server:
  port: 7200
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zhihu?serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
mybatis:
  mapper-locations: classpath:mappers/*.xml # 映射文件的路径扫描
  type-aliases-package: com.example.zhihusecond.dto
